{
  "name": "adiya-portfolio",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy:preview": "npm run build && firebase hosting:channel:deploy preview",
    "deploy": "npm run build && firebase deploy --only hosting"
  },
  "devDependencies": {
    "vite": "^8.0.1"
  },
  "dependencies": {
    "three": "^0.183.2"
  }
}
